import React, { ReactElement } from 'react';
import Ranking from './pages/Ranking.page';

const App = (): ReactElement => {
  return (
    <div className="App">
      <Ranking />
    </div>
  );
};

export default App;
